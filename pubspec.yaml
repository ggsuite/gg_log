name: gg_log
description: Helpers for logging, e.g. a GgLog typedef used in many of our projects.
version: 1.1.0
repository: https://github.com/inlavigo/gg_log.git

environment:
  sdk: ">=3.13.0 <4.0.0"

dependencies:
  path: ^1.9.1

dev_dependencies:
  dna_ggsuite: ^0.2.0
  helix: ^1.2.0
  lints: ^6.1.0
  test: ^1.31.2
